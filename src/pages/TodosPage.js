import Tasks from '../modules/Todolist/components/tasks';
import TodolistAddForm from '../modules/Todolist/components/todolistAddForm';

const TodosPage = () => (
  <div className="page container">
    <Tasks />
  </div>
);

export default TodosPage;
